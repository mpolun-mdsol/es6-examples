{
  "name": "es6-example",
  "version": "1.0.0",
  "description": "",
  "main": "00-let-const-arrow.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src/ -d build/",
    "watch": "babel --watch src/ -d build/",
    "cli": "babel-node"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0"
  }
}
